# 스택 메모

문제 번호	|	소요시간	|				메모
===============================================================
boj_10828	-	1시간30분	-	[스택] string으로 입력 여러 커맨드 받을 경우
								(pop,push등) 딕셔너리로 매핑해두기
								> cmds = { "pop": lambda: out.append(str(st.pop())),... }
